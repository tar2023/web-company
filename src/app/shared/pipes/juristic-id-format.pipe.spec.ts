import { JuristicIDFormatPipe } from './juristic-id-format.pipe';

describe('JuristicIDFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new JuristicIDFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
